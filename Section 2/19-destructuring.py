currencies = 0.8, 1.2
usd, eur = currencies  # 0.8 goes into usd variable, 1.2 goes into eur variable

friends = [("Rolf", 25), ("Anne", 37), ("Charlie", 31), ("Bob", 22), ]

for name, age in friends:
    print(f"{name} is {age} years old.")
